apiVersion: v1
kind: Service
metadata:
  name: {{ include "datacharter.fullname" . }}
  labels:
    {{- include "datacharter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "datacharter.selectorLabels" . | nindent 4 }}
